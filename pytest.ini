[pytest]
# Collect only the real test suite. Archived/experimental trees contain modules
# that exit or import hardware at import time and must never be collected.
testpaths = tests
norecursedirs = archive .git __pycache__ src/protocols/generated src/printing src/agents/archive vision_tests/raw
addopts = -ra
