# Shared paper grid for both complementary v10 protocols.
# Edit this one file when both v10a and v10b should move together.
destination:
  rows: [A, B, C]              # three replicate/response rows
  columns: [1, 2, 3]           # shared paper columns
