# Nanoparticle Printing Optimization Suite — Dependencies
# Install with: pip install -r requirements.txt

opentrons>=7.0,<9.0          # General tools; v3 uses the isolated pinned file
pyyaml>=6.0,<7.0             # YAML config reading/writing
pandas>=1.5                  # CSV / DataFrame operations
numpy>=1.23                  # Numerical computations
matplotlib>=3.6              # Optimization plots
pydantic>=2.0                # Data validation (config dataclasses)
